Base SQLite simples do cardapio.

Arquivos:
- `schema.sql`: estrutura das tabelas
- `seed.sql`: categorias, produtos e relacionamentos
- `cardapio.sqlite`: banco gerado localmente

As imagens dos produtos ficam vazias em `image_src` para preenchimento posterior.
